# English deep — README introduction

## Before
Project X is a command-line tool that runs tests only for files changed in git. Install it with `pip install project-x`, run it with `px test`, and configure it in `px.toml`. The project aims to keep its core implementation under 2,000 lines. Contributions that preserve that constraint are welcome. Project X is a comprehensive, state-of-the-art solution designed to revolutionize the development workflow.

## After (deep)
Project X is a command-line tool for running tests against files changed in git. Install it with `pip install project-x`, then run `px test`. Configuration lives in `px.toml`.

The core is intended to stay under 2,000 lines, so contributions should preserve that constraint.


## Notes
The rewrite keeps commands, configuration, behavior, and the 2,000-line constraint exactly grounded in the source.
